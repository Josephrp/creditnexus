# CreditNexus Project Site

This is the project landing site for CreditNexus, deployed separately to GitHub Pages.

## Development

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

The build output will be in the `dist/` directory, which is automatically deployed to GitHub Pages via GitHub Actions.
